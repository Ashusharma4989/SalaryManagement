import { Component } from '@angular/core';

@Component({
  selector: 'app-users',
  standalone: true,
  template: `
    <section class="users">
      <h1>Users</h1>
      <p>Admin-only user management page (to be implemented).</p>
    </section>
  `,
})
export class UsersComponent {}
