# DB
SPRING_DATASOURCE_URL=
SPRING_DATASOURCE_USERNAME=
SPRING_DATASOURCE_PASSWORD=

# App
SPRING_PROFILES_ACTIVE=dev
SERVER_PORT=8080

# JWT — leave blank for a random-per-startup dev secret; set a stable value in production
APP_JWT_SECRET=

# Dev seed is applied by Flyway migration V2__seed_admin.sql (dev profile):
# inserts a single user admin / admin with role ROLE_ADMIN.
# Edit/re-hash that migration to change or rotate the dev credential.
